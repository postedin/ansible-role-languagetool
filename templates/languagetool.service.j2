[Unit]
Description=LanguageTool Server
Requires=network.target
After=network.target

[Service]
ExecStart=/usr/bin/java -cp /opt/languagetool/LanguageTool-4.0/languagetool-server.jar org.languagetool.server.HTTPServer --port 8010 --public --config /opt/languagetool/server.properties

Type=simple
User=ubuntu
Group=ubuntu

[Install]
WantedBy=multi-user.target
